namespace DefaultNamespace
{
    public enum PanelID
    {
        /* PANELS ACTIVATED FROM MENU BUTTONS*/
        // Top Menu
        Settings,
        // Low Menu
        Roster,
        Profile,
        Dance,
        Shop,
        Missions,
        // Nested in Panel Prefabs (Overlay panels)
        DanceSession,
        DanceSummary
    }
}